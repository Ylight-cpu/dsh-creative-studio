# 更新日志

本文件记录 dsh-creative-studio 的对外变更。版本号遵循语义化版本。

## 0.2.1 — 修复两起「看起来像成品、实际是废片」的事故

这两起事故是真实交付翻车后定位出来的（用户实拍供暖炉素材复现），修复的同时把它们变成了
**可复现的客观闸门**，写进 `selftest`，避免同类问题再次悄悄交付。

### 修复 1 · 视频抠像把主体抠没了（A 版全白只剩字幕）

- **现象**：`video-matte` 默认走 RVM，成片几乎全白，主体覆盖率 0.0000–0.0134，平均 RGB ≈ (254,254,254)。
- **根因**：RVM（Robust Video Matting）是**人像/人体**视频抠像模型。对产品/物体素材，
  `pha` 最大只有 0.29、>0.5 覆盖率 0.0000；同一帧用 rembg `isnet-general-use` 覆盖率 37.97%。
- **修复**：默认后端改为 **rembg 逐帧通用抠像**（`--quality auto` → `isnet-general-use`），
  产品/物体/动物/人都可用；RVM 保留为 `--backend rvm`，仅在画面里有人的素材上使用。
  逐帧推理统一加 alpha 时域平滑（`--alpha-smooth`，默认 0.25）。
- **新增闸门**：主体存在性。统计每帧前景覆盖率，均值低于 `--min-subject`（默认 0.5%）时
  **第 20 帧提前熔断 → 删除产物 → `status: rejected` → 退出码 2**，绝不交付空视频。
  覆盖率 >97% 同样判定异常（背景根本没被去掉）。`--allow-empty` 才可强行输出并标记 `gate_overridden`。
- **实测**：同一 3 秒 720×1280 素材，修复后 `coverage_mean=0.4087`（min 0.154，空帧 0），
  2.3 fps；错误地用 RVM 抠产品时在第 20 帧被拦下，未产出任何文件。

### 修复 2 · 光效把整片染粉 + 漂移的"幽灵"亮斑（B/C 版）

- **现象**：B/C 版全片品红偏色（画面核心区 `R-(G+B)/2` 从源片的 +13 变成 +56…+67），
  并在画面下部看到一个半透明"幽灵人头"状亮斑。
- **根因（两条叠加）**：
  1. `blend=all_mode=screen` 跑在 `yuv420p` 上会混合 UV 平面 → 品红偏色；
  2. `leak` 用了一条**全画面、带品红端点的线性渐变**（`0xff3cac`）并缓慢旋转，
     于是暖/品红亮斑在全画面漂移，扫到画面下部时就成了"幽灵人头"。
- **修复**：
  - 所有 screen/blend 类预设显式 `format=gbrp … format=yuv420p`；
  - `leak` 改为 **`type=radial` 左上角暖色光斑**，外圈近黑，只提亮局部，不再全片加色；
  - 影调取值整体收敛（teal-orange `rs 0.12→0.06`、punch `1.12→1.08`、warm/cool `±0.05`）。
- **新增闸门**：全局偏色。`selftest` 对每个光效输出抽帧，量「相对源片的偏色增量」，|Δ| > 20 即失败。
- **实测**：同一组合（teal-orange + punch + leak）偏色增量由 +45 降到 **+4.2…+7.2**；单独 `leak` 为 +5.8。

### 新增验收工具

- `tests/verify_fix.py` — 一条命令量两件事：抠像主体是否还在（非白像素占比）、光效是否全片染色。
  可脱离插件单独跑：`python tests/verify_fix.py --matte <成片> --fx <成片> --fx-ref <未调色文件>`。
- `selftest` 由 27 项增至 **29 项**，新增「主体闸门判定逻辑」「全局偏色闸门」，
  并把「AI 抠像→白底」的判据从「四角变白」（空片也满足）升级为「四角变白 **且** 主体覆盖率 ≥2%」。
- `tests/rebuild_styles.py` — 端到端重建三版风格样片并在每步过闸门的可复现脚本。

### 其他

- `video-matte` 新增 `--quality`、`--min-subject`、`--allow-empty`、`--mask-scale`、`--post-process-mask`；
  `--model` 现在同时接受 rembg 模型名与 RVM 名。
- 文案时间轴提醒：短视频里每条动效标题至少留 1.2 秒可读时间（旧三版第三条标题只有 1.3 秒且贴着片尾）。

## 0.2.0 — 视频四大能力补齐

- **抠像换背景**（AI / 绿幕色键，三种输出模式）。
- **文字动效**：ASS 生成 + 烧入，`fade / slide-* / typewriter / pop / bounce / karaoke / lower-third`。
- **转场**：53 种原生 `xfade` + 5 种风格化转场（`glitch / whip-pan / flash / soft-zoom / film-burn`）。
- **光效与调色**：13 个预设 + 外部 3D LUT。
- 抠图质量策略升级：`auto` 按实测边缘过渡宽度自动升级到 `birefnet-general`；
  `export --tight` 主体紧裁满足平台「商品占画幅」要求。
- `doctor` 增加 T3 档位、RVM 模型与可用转场数量报告。

## 0.1.0 — 首个版本

- 图片：抠图（T0/T1 分层）、白底主图、局部重绘、文字/logo/水印、多层合成、批量平台规格导出、接触表、配色提取。
- 视频：素材通览、抽帧审片、裁切、拼接、贴文字、字幕、配乐混音、封面选帧、平台规格导出。
- 可移植安装：`bootstrap` / `install-as-plugin` / `install-into-dsh` / `verify-install` / 卸载脚本。
